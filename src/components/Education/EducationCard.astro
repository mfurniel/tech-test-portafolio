---
const { imageSrc, altText, title, description, bgColor } = Astro.props;
---

<div
  class={`flex flex-col items-center justify-start p-10 rounded-2xl gap-4 ${bgColor}`}
>
  <img src={imageSrc} alt={altText} class="h-28" />
  <h2 class="text-2xl">{title}</h2>
  <p class="text-center text-lg">{description}</p>
</div>
